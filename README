This project contains botrunner program which is used in conjuction with the springgrid website to run spring instances of matches scheduled there.
To run the botrunner go into the botrunner directory and run "botrunner.py" - you might need to set up some configurations if you haven't previously.
